/*
 * Defines a camera class
 *
 * @author Arman Uguray
 */
 
/*
    #include "Sylvester.js"
    #include "weblglu.js"
    // Vector3 is a 3d Sylvester Vector
    // Vector4 is a 4d Sylvester Vector
    
    /*
     * This class is a convenience wrapper around the 
     * WebGLU camera. 
     */
/*
    Camera class
 
    IDL:
    [
        Constructor(double width, double height);
    ]
    
    interface Camera
    {
        // These return the primary 4x4 camera matrices
        Matrix getProjectionMatrix();
        Matrix getModelviewMatrix();
        
        // Implementation of gluLookAt
        void lookAt(Vector3 eye, Vector3 center, Vector3 up);
        
        // Returns the position of the camera
        Vector4 getEye()
        
        // These set camera projection attributes
        void setFOV(double fov);
        void setClip(double nearPlane, double farPlane); 
        void setSize(double width, double height);
        
        // Translates the camera along its look vector
        void lookVectorTranslate(double delta);
    }
*/


    